# Hub GUI v0 QA Run - 2026-06-24

QA source: [`docs/qa/hub-gui-v0.md`](../hub-gui-v0.md)

Scope: GitHub issues #151 to #156.

Branch: `gui-v0`

Commit: `36eb7b7`

Environment:

- OS: macOS host
- Node: `v20.20.2`
- npm: `10.8.2`
- Git status before QA included local runtime noise: `.beads/interactions.jsonl`
- QA created `hub-desktop/package-lock.json` after following the documented `npm install` step.

## Summary

Result: **Fail**

Reason: Core package checks and model-level tests passed, but `hub-desktop` fails both renderer typecheck and production build. Desktop launch smoke and real project GUI QA were not attempted after those blocking automated gates failed.

Issues created:

- #160: Hub desktop Task Board typecheck imports `HubTaskStatus` from a non-exporting contract
- #161: Hub desktop production build bundles Node-only archLoop modules into the renderer
- #162: Hub desktop install creates an untracked lockfile and reports critical npm audit findings

## Automated Gates

| Gate                                 | Result             | Evidence                                                                                                |
| ------------------------------------ | ------------------ | ------------------------------------------------------------------------------------------------------- |
| `npm run typecheck`                  | Pass               | `tsgo --noEmit` exited 0                                                                                |
| Targeted GUI model tests             | Pass               | 8 files, 66 tests passed                                                                                |
| `npm run build`                      | Pass               | root build and postbuild completed                                                                      |
| `hub-desktop/npm install`            | Pass with findings | install completed, created untracked `hub-desktop/package-lock.json`, reported 58 audit vulnerabilities |
| `hub-desktop/npm run typecheck`      | Fail               | `TS2305` for `HubTaskStatus` import                                                                     |
| `hub-desktop/npm run build`          | Fail               | Vite renderer build failed on Node-only import graph                                                    |
| `hub-desktop/npm run build:electron` | Pass               | Electron main/preload TypeScript build completed                                                        |

Targeted GUI model test command:

```bash
npx vitest run \
  src/hubDesktopLaunch.test.ts \
  src/hubDesktopShell.test.ts \
  src/hubRuntimeBridge.test.ts \
  src/hubProjectOverview.test.ts \
  src/hubRunWorkbench.test.ts \
  src/hubProposalSession.test.ts \
  src/hubProposalWorkbench.test.ts \
  src/hubTaskBoardWorkbench.test.ts
```

Result:

```text
Test Files  8 passed (8)
Tests       66 passed (66)
```

## Finding 1: Desktop Typecheck Fails

Issue: #160

Command:

```bash
cd hub-desktop
npm run typecheck
```

Actual:

```text
src/components/views/TaskBoardView.tsx(16,15): error TS2305:
Module '"@yibeibankaishui/archloop/hub-runtime-contract"' has no exported member 'HubTaskStatus'.
```

Impact:

- Blocks `hub-desktop` TypeScript validation.
- Indicates the runtime contract and desktop renderer imports are out of sync.

## Finding 2: Desktop Production Build Fails

Issue: #161

Command:

```bash
cd hub-desktop
npm run build
```

Actual:

```text
error during build:
../dist/resolveBdExecutable.js (4:9): "createRequire" is not exported by "__vite-browser-external",
imported by "../dist/resolveBdExecutable.js".
```

Vite also emitted many browser externalization warnings for Node built-ins pulled from `../dist/*`, including `node:fs`, `node:path`, `node:module`, `node:child_process`, `node:http`, and `node:net`.

Impact:

- Blocks production desktop app build.
- Blocks Desktop Launch Smoke and real project GUI QA after automated gates.
- Suggests renderer imports are reaching Node-only runtime/service modules.

Control check:

```bash
cd hub-desktop
npm run build:electron
```

Result: pass. Electron main/preload build works, so the blocking build failure is concentrated in the renderer bundle path.

## Finding 3: Dependency Hygiene And Audit Findings

Issue: #162

Command:

```bash
cd hub-desktop
npm install
npm audit --audit-level=critical
```

Actual:

```text
58 vulnerabilities (1 low, 42 moderate, 12 high, 3 critical)
```

Critical packages reported:

- `protobufjs <=7.6.2`
- `shell-quote 1.1.0 - 1.8.3`
- `vitest <3.2.6`

High severity packages included `electron`, `axios`, `undici`, `ws`, `vite`, and others.

`npm install` also created:

```text
?? hub-desktop/package-lock.json
```

Impact:

- Fresh desktop dependency install is not deterministic from a committed lockfile.
- QA install dirties the worktree.
- Critical/high audit findings need to be fixed or explicitly documented as non-runtime/dev-only exceptions.

## Manual QA Status

| Area                         | Result       | Notes                                                                                     |
| ---------------------------- | ------------ | ----------------------------------------------------------------------------------------- |
| #151 desktop target          | Blocked      | Electron app exists, but desktop build gate failed before launch smoke                    |
| #152 shell/bridge            | Partial pass | Unit tests and Electron main/preload build pass; renderer app cannot pass typecheck/build |
| #154 overview                | Partial pass | Model tests pass; visual desktop QA blocked                                               |
| #156 task board              | Fail         | Typecheck fails in `TaskBoardView.tsx`                                                    |
| #153 run workbench           | Partial pass | Model tests pass; visual desktop QA blocked                                               |
| #155 proposal session        | Partial pass | Model/session tests pass; renderer build pulls Node-only modules                          |
| responsive layout            | Not run      | Blocked by desktop automated gates                                                        |
| keyboard/accessibility smoke | Not run      | Blocked by desktop automated gates                                                        |

## Release Decision

Do not pass Hub GUI v0 QA yet.

Minimum fixes before rerun:

1. Close #160 so `hub-desktop/npm run typecheck` passes.
2. Close #161 so `hub-desktop/npm run build` passes.
3. Resolve or explicitly triage #162 so dependency install and audit policy are reproducible.

After those are fixed, rerun the same QA document from the top and continue into fixture and real-project desktop launch smoke.
