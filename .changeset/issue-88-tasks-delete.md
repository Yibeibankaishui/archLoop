---
"@yibeibankaishui/archloop": patch
---

Add `archloop tasks delete` for destructive local Beads task removal with selector resolution, batch delete, TTY confirmation, `--yes` / `--dry-run` guards, `--cascade` passthrough, and post-delete Beads verification.
