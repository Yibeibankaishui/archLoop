---
"@yibeibankaishui/archloop": patch
---

Initialize new Hub projects with a Hub-owned Beads task store under the stable Hub project directory, and install a Git-ignored `.beads/redirect` so host `bd` commands resolve the same store. Existing repository-local stores are left in place.
