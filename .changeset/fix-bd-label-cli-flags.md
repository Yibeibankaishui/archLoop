---
"@ai-hero/sandcastle": patch
---

Fix Hub Beads task board commands to use `bd update` label flags compatible with bundled `@beads/bd@1.0.4` (`--add-label` / `--remove-label` instead of `--add-labels` / `--remove-labels`).
