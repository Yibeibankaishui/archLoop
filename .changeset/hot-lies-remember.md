---
"@yibeibankaishui/archloop": patch
---

Add the Hub task-board read path: `archloop tasks list` groups Beads tasks by canonical Hub status and `archloop tasks show <id>` renders task details, labels, metadata, comments, remote refs, and run refs. Update the CLI docs and bundled usage skill to match the new task-board commands.
