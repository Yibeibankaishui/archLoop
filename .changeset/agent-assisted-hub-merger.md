---
"@yibeibankaishui/archloop": patch
---

Add agent-assisted conflict resolution to Hub batch merge so `archloop run . --flow with-review` can invoke the configured merge role before marking merge conflicts as failed.
