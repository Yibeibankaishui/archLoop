---
"@ai-hero/sandcastle": patch
---

Add agent-assisted conflict resolution to Hub batch merge so `sandcastle run . --flow with-review` can invoke the configured merge role before marking merge conflicts as failed.
