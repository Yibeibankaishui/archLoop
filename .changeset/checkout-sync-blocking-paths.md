---
"@yibeibankaishui/archloop": patch
---

Persist exact dirty-checkout blocking paths on checkout projection outbox items and expose them in task results, plain output, JSON output, and live `checkout_sync_pending` events without changing shipped or rewriting local WIP.
