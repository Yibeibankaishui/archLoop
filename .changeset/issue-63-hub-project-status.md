---
"@yibeibankaishui/archloop": patch
---

Add `archloop project status` so archLoop can report the canonical git repo root, the resolved user data directory, Hub registration state, `bd` availability, and initial task-board counts from any git repo without requiring `archloop init`.
