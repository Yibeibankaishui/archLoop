---
"@ai-hero/sandcastle": patch
---

Add `sandcastle project status` so Sandcastle can report the canonical git repo root, the resolved user data directory, Hub registration state, `bd` availability, and initial task-board counts from any git repo without requiring `sandcastle init`.
