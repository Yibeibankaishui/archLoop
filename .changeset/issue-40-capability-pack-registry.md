---
"@yibeibankaishui/archloop": patch
---

Add a capability pack registry with `generic` and `miniprogram` definitions, `--capability` / `--capability-addons` init flags, default resolution before explicit init overrides, and optional `.archloop/capability.json` scaffold metadata when a pack is selected explicitly.
