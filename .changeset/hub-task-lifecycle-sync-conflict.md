---
"@yibeibankaishui/archloop": patch
---

Route Hub task sync conflict status changes through the Hub task lifecycle module so sync conflict metadata, labels, and completed-task preservation are centralized instead of duplicated in task sync.
