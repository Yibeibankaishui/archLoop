---
"@yibeibankaishui/archloop": patch
---

Add agent auth failure guidance that steers users toward `archloop env init`, `archloop env set`, and `.archloop/.env` instead of agent login commands. Enrich Orchestrator AgentError messages and fail fast in Hub proposal and flow agent preflight checks.
