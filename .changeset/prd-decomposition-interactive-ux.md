---
"@ai-hero/sandcastle": patch
---

Improve `tasks from-prd` interactive UX: show each proposal phase in the terminal with log paths, use confirm prompts for refinement, and fix approval being skipped when `--yes` is not set.
