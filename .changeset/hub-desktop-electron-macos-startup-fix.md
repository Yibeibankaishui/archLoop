---
"@yibeibankaishui/archloop": patch
---

Bump the committed `hub-desktop` Electron runtime to the fixed `41.7.2`
release and tighten the desktop launch regression so the macOS fixture smoke
stays pinned to the point release that avoids the startup SIGABRT.
