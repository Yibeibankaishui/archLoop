---
"@yibeibankaishui/archloop": patch
---

Replace the interactive `archloop run` confirmation with a `section` run plan and a 3-second debounced start (`Ctrl+C` cancel, `e` edit flow). Add `--dry-run` to render the plan without starting.
