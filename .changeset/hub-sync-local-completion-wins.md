---
"@ai-hero/sandcastle": patch
---

Redesign Hub GitHub task exchange with explicit `sandcastle tasks pull` and `tasks push` commands, open-only pull defaults, `sync --dry-run` preview plus guarded `sync --yes` application, duplicate same-title candidate reporting, metadata-stored GitHub refs, and local `done` / `wontfix` closures that are pushed to GitHub without being downgraded by stale remote labels.
