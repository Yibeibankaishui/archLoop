---
"@ai-hero/sandcastle": patch
---

Implement the native Mini Program static fallback verifier for projects without `wx:check`, including project config selection, unsupported variant rejection, and app/page/component/tabBar structure checks with JSONL diagnostics.
