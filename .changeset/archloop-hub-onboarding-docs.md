---
"@yibeibankaishui/archloop": patch
---

Update the Hub onboarding and readiness docs to center `archloop initialize`,
`archloop check`, `archloop project add` / `select` / `list`, and selected-project
`archloop run --flow ...`, and refresh the bundled `skills/archloop-usage/SKILL.md`
so it treats `archloop init` as the legacy repo-local scaffold path.
