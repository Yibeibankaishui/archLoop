---
"@yibeibankaishui/archloop": patch
---

Add project-targeted `archloop check` readiness slices for the selected project, an explicit project, or all registered projects. The check now reports repo path, git, initial commit, development contract, task store, task summary, active run, and flow readiness signals while preserving the existing Hub-wide smoke checks.
