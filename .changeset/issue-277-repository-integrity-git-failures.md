---
"@yibeibankaishui/archloop": patch
---

Report corrupt Git refs and objects as repository-integrity failures during merge selection and task-state doctoring, preserving Git diagnostics and recovery guidance instead of mislabeling them as missing branches or empty diffs.
