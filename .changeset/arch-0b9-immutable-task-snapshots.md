---
"@yibeibankaishui/archloop": patch
---

Run Hub implementation and review agents from an immutable, task-scoped snapshot instead of the live Beads store. Structured `<task-notes>` are schema-validated and applied by Hub after each attempt.
