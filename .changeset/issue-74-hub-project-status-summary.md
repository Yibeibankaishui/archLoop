---
"@yibeibankaishui/archloop": patch
---

Enhance `archloop project status` into a CLI-first Hub task board summary: task counts by Hub status, active runs and batch statuses, failed tasks with failure reason and next action, sync state summaries such as `push_pending` or `conflict`, recent Hub events, and paths to Hub run directories for full logs and artifacts.
