---
"@ai-hero/sandcastle": patch
---

Scaffold Sandcastle scripts with `npm exec --yes --package tsx -- tsx` so the workflow runner is less likely to use a broken project-local `tsx` or `esbuild` binary.
