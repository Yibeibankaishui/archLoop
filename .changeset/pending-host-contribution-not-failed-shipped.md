---
"@yibeibankaishui/archloop": patch
---

Report host-contribution merge conflicts and other all-pending landings as `pending` / `completed_with_pending_merge` instead of counting them as shipped or failed, and distinguish `host_contribution_conflict` from `target_quiet_wait`.
