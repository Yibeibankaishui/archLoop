---
"@ai-hero/sandcastle": patch
---

Wire `sandcastle run --flow prd-decomposition` and `sandcastle run --flow triage` to the same agent-driven proposal flows as the task shortcuts. Deterministic PRD and triage helpers remain test-only fallbacks.
