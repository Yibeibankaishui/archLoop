---
"@yibeibankaishui/archloop": patch
---

Preserve registered Hub project directories through `archloop run --project` resolution so runs write artifacts under the selected project's registry storage instead of the path-hash fallback.
