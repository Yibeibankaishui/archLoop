---
"@yibeibankaishui/archloop": patch
---

Add the `archloop init --runtimes` flag for non-interactive multi-runtime selection, with early unknown-runtime validation and default single-runtime behavior when omitted alongside `--agent`.
