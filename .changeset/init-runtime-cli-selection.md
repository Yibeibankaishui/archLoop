---
"@ai-hero/sandcastle": patch
---

Add the `sandcastle init --runtimes` flag for non-interactive multi-runtime selection, with early unknown-runtime validation and default single-runtime behavior when omitted alongside `--agent`.
