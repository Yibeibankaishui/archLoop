---
"@yibeibankaishui/archloop": patch
---

Complete synthetic `hub-host-contribution` landings during reconciliation without Beads close lookup or `tasks recover`, so runs can append `run_completed` after importing committed host-target work.
