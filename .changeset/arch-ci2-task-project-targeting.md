---
"@yibeibankaishui/archloop": patch
---

Make Hub task commands target the selected Hub project by default, add `--project <name>` as an explicit override across task-board flows, and keep task selectors working against the chosen project. Update the README, bundled usage skill, and user guides to match.
