---
"@yibeibankaishui/archloop": patch
---

Task-board Hub flows now continue to the next ready batch after a successful batch completes, reload the ready queue between batches, report aggregate batch/task counts across the whole run, and honor `--max-batches` as a total batch cap.
