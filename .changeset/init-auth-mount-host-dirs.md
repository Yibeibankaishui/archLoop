---
"@ai-hero/sandcastle": patch
---

Create scaffold auth host directories under `.sandcastle/auth/*` during `sandcastle init`, based on the selected installed runtime auth mounts and selected backlog manager auth mounts.
