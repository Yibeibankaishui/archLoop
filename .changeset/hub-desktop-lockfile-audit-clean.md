---
"@yibeibankaishui/archloop": patch
---

Commit the `hub-desktop` lockfile and document `npm ci` as the deterministic
install path so QA no longer generates an untracked lockfile, and keep the
desktop dependency tree audit-clean with the updated Electron resolution.
