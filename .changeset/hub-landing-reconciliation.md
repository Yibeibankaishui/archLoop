---
"@yibeibankaishui/archloop": patch
---

Resume interrupted Hub landing transactions from durable physical evidence at run startup and completion. Valid candidates, verification artifacts, atomic receipts, and matching Beads close metadata are reused; cleanup retries independently; `tasks doctor` stays read-only.
