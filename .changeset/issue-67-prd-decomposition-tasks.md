---
"@yibeibankaishui/archloop": patch
---

Add `archloop tasks from-prd <prd-ref>` to draft tracer-bullet vertical slices from local PRDs, classify each slice as AFK or HITL, confirm dependency pairs and initial Hub status, and create Beads tasks with `prd-decomposition` origin metadata plus `bd dep add` edges. README, bundled usage skill, and roadmap are updated to match.
