---
"@yibeibankaishui/archloop": patch
---

Planned Hub batch selection now recovers false `explicit_blocker` deferrals using live board descriptions and blocker metadata, keeps valid selected-task dependencies deferred, and still honors `maxTasks`.
