---
"@yibeibankaishui/archloop": patch
---

Add the first Hub project registry slice: `archloop project add`, `project list`, and `project select` now persist shared Hub projects with stable ids, mark the selected Hub project, and work from any directory instead of inferring a project from `cwd`.
