---
"@yibeibankaishui/archloop": patch
---

Hub batch merge now routes merge, verification, close, and revert outcomes through the shared Hub task lifecycle module so task status policy stays centralized while merge sequencing behavior is unchanged.
