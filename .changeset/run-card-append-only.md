---
"@yibeibankaishui/archloop": patch
---

Rewrite Hub run live view as append-only `section` snapshots with a single-line `clack.spinner` heartbeat (ADR-0032 / Variant C Phase 3a). Run and batch ids display as 8-char prefixes.
