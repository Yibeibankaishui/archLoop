# WeChat Mini Program context

For WeChat Mini Program work:

- Respect `app.json` / page JSON for routing, `window`, and `tabBar`.
- Prefer official APIs and documented lifecycle hooks over undocumented behavior.
- For CloudBase + 云开发, follow project conventions for `wx.cloud` usage.
